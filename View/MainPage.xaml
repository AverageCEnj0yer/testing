<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="testing.View.MainPage"
             xmlns:controls="clr-namespace:testing.View"
             Title="Home">

    <Grid RowDefinitions="*, Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout
                Padding="30,0"
                Spacing="25">

                <Label
                    Text="Hello from MainPage!"
                    FontSize="20"
                    HorizontalOptions="Center"
                    VerticalOptions="Center" />

            </VerticalStackLayout>
        </ScrollView>

        <controls:CustomNavBarView Grid.Row="1"/>
    </Grid>
</ContentPage>